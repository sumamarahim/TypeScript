let firstName = "sumama";

// type annotations on variables
// when we declare a var using let, const, or var, we can optionally add explicitly specify the type of var

const name1: String = "ali";

console.log(firstName);
